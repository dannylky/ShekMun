# logs
monitoring/logs/

# runtime snapshot (regenerated by monitor)
monitoring/snapshot.json

# temperature snapshot (regenerated by temp-monitor.py)
monitoring/temp-snapshot.json

# device credentials (never commit real passwords)
monitoring/creds.json

# build artifacts (rebuilt by build-deploy.ps1)
monitoring/*.exe
deploy/

# full backup copies (each contains its own .git - never commit)
Backup/

# OS noise
.DS_Store
Thumbs.db
